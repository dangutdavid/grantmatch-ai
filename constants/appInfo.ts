import { brand } from '@/constants/brand';

export const appInfo = {
  appName: brand.appName,
  version: '1.0.0',
  environment: 'mock/local',
  platformSupport: ['Web', 'iOS', 'Android'],
  buildStatus: 'demo-ready',
};
